# Azure Service Bus Configuration
# Replace these values with your actual Azure Service Bus details
# Uses DefaultAzureCredential for authentication (no connection string needed)

# Service Bus Namespace (without .servicebus.windows.net)
AZURE_SERVICEBUS_NAMESPACE=your-servicebus-namespace

# Service Bus Topic Name
AZURE_SERVICEBUS_TOPIC_NAME=customer-events

# Connection timeout in seconds
AZURE_SERVICEBUS_CONNECTION_TIMEOUT_SECONDS=30

# Database Configuration (if you want to override)
# DATABASE_URL=jdbc:postgresql://localhost:5432/customerdb
# DATABASE_USERNAME=postgres
# DATABASE_PASSWORD=postgres123

# OpenTelemetry Configuration
OTEL_SERVICE_NAME=customer-api
OTEL_SERVICE_VERSION=1.0.0
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:18889

# Application Configuration
SERVER_PORT=8090
