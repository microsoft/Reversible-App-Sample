<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender for standard logging -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- OpenTelemetry appender for sending logs to OTLP -->
    <appender name="OTEL" class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
    </appender>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="OTEL"/>
    </root>

    <!-- Application-specific logging -->
    <logger name="com.revapps.customerapi" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="OTEL"/>
    </logger>

    <!-- Spring Framework logging -->
    <logger name="org.springframework" level="INFO"/>
    
    <!-- Hibernate/JPA SQL logging -->
    <logger name="org.hibernate.SQL" level="DEBUG"/>
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE"/>

    <!-- Dapr client logging -->
    <logger name="io.dapr" level="DEBUG"/>
    
    <!-- OpenTelemetry logging -->
    <logger name="io.opentelemetry" level="INFO"/>
</configuration>
